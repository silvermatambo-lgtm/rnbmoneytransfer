import {useEffect,useState} from 'react';
import {Link,useLocation} from 'react-router-dom';
import {Menu,X,KeyRound} from 'lucide-react';
const links=[['Home','/'],['Send Money','/send'],['Rates','/rates'],['About','/about'],['How It Works','/how-it-works'],['Contact','/contact']];
export default function Navbar(){const [open,setOpen]=useState(false);const [scrolled,setScrolled]=useState(false);const {pathname}=useLocation();useEffect(()=>{const fn=()=>setScrolled(scrollY>30);addEventListener('scroll',fn,{passive:true});return()=>removeEventListener('scroll',fn)},[]);useEffect(()=>setOpen(false),[pathname]);return <>
<header className={`fixed top-0 inset-x-0 z-50 transition-all ${scrolled||pathname!=='/'?'bg-white/95 backdrop-blur shadow-lg':'bg-white/90 backdrop-blur'}`}><div className="max-w-7xl mx-auto px-4 h-20 flex items-center justify-between gap-4"><Link to="/" className="flex items-center min-w-0"><img src="/images/logo.png" className="h-16 w-auto object-contain max-w-[210px]" alt="R&B Money Transfer"/></Link><nav className="hidden xl:flex items-center gap-1">{links.map(([l,t])=><Link key={t} to={t} className={`px-3 py-2 rounded-lg text-sm font-bold ${pathname===t?'bg-emerald-50 text-emerald-700':'text-slate-700 hover:text-emerald-700'}`}>{l}</Link>)}</nav><div className="flex items-center gap-2"><Link to="/admin" className="hidden md:inline-flex items-center gap-2 px-4 py-2.5 rounded-xl bg-slate-950 text-white font-bold text-sm"><KeyRound size={16}/> Admin Login</Link><button onClick={()=>setOpen(true)} className="xl:hidden p-2"><Menu/></button></div></div></header>
<div className={`fixed inset-0 z-[60] ${open?'pointer-events-auto':'pointer-events-none'}`}><div onClick={()=>setOpen(false)} className={`absolute inset-0 bg-black/60 transition ${open?'opacity-100':'opacity-0'}`}/><aside className={`absolute right-0 top-0 h-full w-[86%] max-w-sm bg-white p-5 transition-transform ${open?'translate-x-0':'translate-x-full'}`}><div className="flex justify-between items-center border-b pb-4"><img src="/images/logo.png" className="h-14 w-auto"/><button onClick={()=>setOpen(false)}><X/></button></div><nav className="py-5 space-y-1">{links.map(([l,t])=><Link key={t} to={t} className="block px-4 py-3 rounded-xl font-semibold hover:bg-emerald-50">{l}</Link>)}</nav><Link to="/admin" className="btn-dark w-full"><KeyRound size={17}/> Admin Login</Link></aside></div>
</>}
